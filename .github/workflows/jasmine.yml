name: Run Jasmine

on:
  workflow_call:
    inputs:
      useWithRails:
        description: 'Enable for use with Rails'
        required: false
        default: false
        type: boolean
    secrets:
      BUNDLER_GITHUB_TOKEN:
        required: false
        description: "Token used for Bundler to authenticate when installing gems from private Github repos"

jobs:
  run-jasmine:
    name: Run Jasmine
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          show-progress: false

      - name: Setup Ruby
        if: inputs.useWithRails
        uses: ruby/setup-ruby@d45b1a4e94b71acab930e56e79c6aa188764e7f9 # v1.316.0
        with:
          bundler-cache: true
        env:
          BUNDLE_GITHUB__COM: "x-access-token:${{ secrets.BUNDLER_GITHUB_TOKEN }}"

      - name: Setup Node
        uses: alphagov/govuk-infrastructure/.github/actions/setup-node@bbc8def482c8912b9cdeaee5d3e5d1669843a74f # main

      - name: Precompile Rails assets
        if: inputs.useWithRails
        uses: alphagov/govuk-infrastructure/.github/actions/precompile-rails-assets@bbc8def482c8912b9cdeaee5d3e5d1669843a74f # main

      - name: Run Jasmine
        run: yarn run jasmine-browser-runner runSpecs
